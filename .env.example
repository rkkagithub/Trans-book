# Environment Variables for TransBook

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/transbook

# Session Security
SESSION_SECRET=your-very-secure-random-string-here

# Authentication Configuration
REPL_ID=your-repl-id
REPLIT_DOMAINS=localhost:5000,your-domain.com
ISSUER_URL=https://replit.com/oidc

# Server Configuration
NODE_ENV=production
PORT=5000
HOST=0.0.0.0

# PostgreSQL Database Details (for manual setup)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=password
PGDATABASE=transbook